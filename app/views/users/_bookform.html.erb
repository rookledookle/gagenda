<%= simple_form_for(@course) do |f| %>
  You are signed in as <%= current_user.name %>, would you like to book yourself for the course <%= @course.name %>?
  
  <%= f.association :users, as: :select %>
  <%= f.submit %>
<% end %>